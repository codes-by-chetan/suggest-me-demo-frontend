
  # Content Recommendation Platform

  This is a code bundle for Content Recommendation Platform. The original project is available at https://www.figma.com/design/OpcLlq8BEc4CURueMiF4v7/Content-Recommendation-Platform.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  